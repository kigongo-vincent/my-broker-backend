services:
  database:
    image: postgres
    container_name: my-broker-db6

    environment:
      - POSTGRES_DB=my_broker_db4
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=Admin@2025 
    
    ports:
      - "5433:5432"